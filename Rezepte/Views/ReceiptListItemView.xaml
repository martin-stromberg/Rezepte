<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Rezepte.Views.ReceiptListItemView"
             xmlns:vm="clr-namespace:Rezepte.ViewModels;assembly=RezepteMaui"
             x:DataType="vm:ReceiptListItemViewModel">
    <Frame Margin="5" WidthRequest="150" HeightRequest="200">
        <Grid RowSpacing="0" ColumnSpacing="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>

            <Image 
                Grid.Row="0"
                Grid.RowSpan="3"
                Grid.Column="0"
                Grid.ColumnSpan="2" 
                Source="{Binding Picture}">
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Tapped="ListItem_Clicked"/>
                </Image.GestureRecognizers>
            </Image>
            <VerticalStackLayout
                Grid.Row="3"
                Grid.RowSpan="4"
                Grid.Column="0"
                Grid.ColumnSpan="2">
                <Label
                    Text="{Binding Name}"                    
                    HorizontalOptions="Start" />
            </VerticalStackLayout>
        </Grid>
    </Frame>
</ContentView>
