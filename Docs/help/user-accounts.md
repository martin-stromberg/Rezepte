# Benutzerkonten

## Registrierung

Die Registrierung prÃ¼ft Benutzernamen serverseitig. Ein Benutzername muss 3 bis 20 Zeichen lang sein und darf nur Buchstaben, Zahlen, Unterstrich und Bindestrich enthalten.

Leerzeichen, Emojis, Domains, IP-Adressen und sonstige Sonderzeichen werden abgelehnt. Reservierte oder offiziell wirkende Namen wie `admin`, `root`, `support`, `security_admin` oder appbezogene Namen wie `rezepte` kÃ¶nnen nicht verwendet werden.

Wenn ein Benutzername nicht erlaubt ist oder bereits vergeben wurde, zeigt die Anwendung eine deutschsprachige Fehlermeldung an. WÃ¤hlen Sie in diesem Fall einen anderen Namen.

## Profil

Im Profil kann der eigene Benutzername geÃ¤ndert werden. FÃ¼r die Ã„nderung gelten dieselben Regeln wie bei der Registrierung.

Nach einer erfolgreichen Ã„nderung kann eine erneute Anmeldung nÃ¶tig sein, damit der neue Name in der Navigation angezeigt wird.

## Benutzerverwaltung fÃ¼r Administratoren

Administratoren kÃ¶nnen Benutzer in den Einstellungen unter `Benutzer` anlegen und bearbeiten. Auch dort werden Benutzernamen serverseitig mit denselben Regeln geprÃ¼ft.

Die PrÃ¼fung ersetzt nicht die EindeutigkeitsprÃ¼fung. Ein technisch gÃ¼ltiger Benutzername wird weiterhin abgelehnt, wenn er bereits von einem anderen Benutzer verwendet wird.
